{
  "compilerOptions": {
    "outDir": "./build/", // 트랜스파일한 최종 결과물이 저장되는 경로를지정
    "allowJs": true,  // 트랜스파일할 대상에 .js, .jsx와 같은 파일도 포함
    "esModuleInterop": true,  // ES6가 아닌 commonJS 는 모듈을 임포트하고 익스포트하는 방법이 다름. 이를 해소하기 위해 이 속성을 true 로 지정
    "resolveJsonModule": true,  // 이 속성을 true로 지정하면 .json 텍스트 파일을 자바스크립트 객체로 임포트 가능
    "sourceMap": true,  // 트랜스파일한 코드와 함께 디버깅을 위한 .js.map 과 같은 소스 맵 파일을 생성
    "noImplicitAny": true,  // 암시적 데이터 형식을 사용하지 않기 위해 필수적으로 true 로 지정
    "module": "commonjs", // 컴파일된 결과물이 사용하게 될 모듈 시스템 방식을 지정
    "target": "es5",  // 트랜스파일한 최종 결과물의 형태를 지정. es5로 지정하면 ES6 문법으로 작성한 코드는 모두 ES5 로 트랜스파일됨
    "jsx": "react"
  },
  "include": ["./src/**/*"],  // 트랜스파일할 대상 파일을 지정
  "exclude": ["node_modules", "**/*.spec.ts"] // 트랜스파일할 때 배제 대상 지정
}
